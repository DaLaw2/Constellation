# Constellation v1.1.1 Release Notes

Bug fix release addressing UI issues, performance improvements, and search behavior corrections.

## 🐛 Bug Fixes

### 🔍 Search & Filter
- **Search requires tag selection**: Filename-only search is no longer allowed. At least one tag must be selected to perform a search.
- **CQL requires tag condition**: CQL queries must include a `tag` condition.
- **Filter excludes directories**: When File Types or Size filter is active, directories are now properly excluded from results.
- **Panel mode preserved**: Clicking search results no longer unexpectedly switches the left panel mode.

### 🏷️ Tag Management
- **Tag refresh after operations**: Tags now properly refresh after delete, merge, or move operations.
- **CreateTagDialog spacing**: Fixed suggestions dropdown overlapping with dialog footer.
- **Duplicate template validation**: Template Manager now prevents creating templates with duplicate names.
- **Selection cleared after actions**: Tag table selection is now cleared after executing Move/Merge/Delete operations.

### ⚡ Performance
- **Improved tag loading**: Significantly reduced API calls when browsing directories with many files.

### 🧹 Improvements
- **Remove unused context menu items**: Removed non-functional menu items.
- **Delete confirmation dialog**: File deletion now uses a confirmation dialog instead of toast notification.

## 📦 Installation
Download the Windows installer and run it directly. This is a drop-in replacement for v1.1.0.
